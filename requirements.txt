# ============================================
# RECEPTION ROBOT - REQUIREMENTS
# ============================================

# ============================================
# SIMULATION MODE (No hardware needed)
# ============================================
# These packages come pre-installed with Python:
# - tkinter (GUI)
# - queue (inter-node communication)
# - threading (parallel execution)

# No pip install needed for simulation!

# ============================================
# HARDWARE MODE (When connecting real devices)
# ============================================

# Computer Vision & Camera
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0

# Image Processing
numpy>=1.24.0
Pillow>=10.0.0

# Raspberry Pi GPIO (only on Raspberry Pi)
# RPi.GPIO>=0.7.1

# Serial Communication (for Arduino/external microcontrollers)
# pyserial>=3.5

# Advanced Face Recognition (optional)
# face-recognition>=1.3.0
# dlib>=19.24.0

# Speech Recognition (optional)
# SpeechRecognition>=3.10.0
# pyaudio>=0.2.13

# Text-to-Speech (optional)
# pyttsx3>=2.90
# gTTS>=2.3.0

# Web Interface (optional - for remote control)
# flask>=2.3.0
# flask-socketio>=5.3.0

# Data Visualization (optional)
# matplotlib>=3.7.0

# ============================================
# INSTALLATION COMMANDS
# ============================================

# For simulation mode (no installation needed):
# Just run: python main.py

# For hardware mode on Ubuntu/Debian:
# sudo apt-get update
# sudo apt-get install -y python3-opencv python3-tk
# pip install opencv-python numpy --break-system-packages

# For Raspberry Pi:
# sudo apt-get update
# sudo apt-get install -y python3-opencv python3-rpi.gpio python3-tk
# pip install opencv-python numpy RPi.GPIO --break-system-packages

# Enable camera on Raspberry Pi:
# sudo raspi-config
# Navigate to: Interface Options -> Camera -> Enable

# ============================================
# TESTING INSTALLATION
# ============================================
# Run this to test your setup:
# python -c "import tkinter; print('✓ Tkinter OK')"
# python -c "import cv2; print('✓ OpenCV OK')"
# python -c "import numpy; print('✓ NumPy OK')"


# ══════════════════════════════════════════════════════
# FaceRecognitionNode v2.0 — Requirements
# ══════════════════════════════════════════════════════

# Core — obligatoire
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0          # YOLOv8s + ByteTrack intégré

# Reconnaissance faciale — choisir UN des deux :

# Option A : InsightFace ArcFace (RECOMMANDÉ — CPU puissant)
insightface>=0.7.3
onnxruntime>=1.16.0         # CPU
# onnxruntime-gpu>=1.16.0   # décommenter si GPU NVIDIA disponible

# Option B : dlib via face-recognition (fallback automatique)
face-recognition>=1.3.0
# dlib est installé automatiquement avec face-recognition

# ByteTrack (inclus dans ultralytics, mais lapx nécessaire)
lapx>=0.5.2

# ══════════════════════════════════════════════════════
# Installation complète :
#   pip install -r requirements.txt
#
# Si InsightFace échoue à télécharger buffalo_l automatiquement :
#   mkdir -p ~/.insightface/models/
#   # Télécharger manuellement buffalo_l.zip depuis :
#   # https://github.com/deepinsight/insightface/releases
#   # et décompresser dans ~/.insightface/models/buffalo_l/
# ══════════════════════════════════════════════════════
